import React from 'react'
import ProfileLayout from '../Layouts/ProfileLayout'

const ProfileHome = ({darkMode}) => {
  return (
    <ProfileLayout darkMode={darkMode}>

    <div>
      hello akakk
    </div>
    </ProfileLayout>
  )
}

export default ProfileHome
