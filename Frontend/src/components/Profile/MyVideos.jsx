import React from "react";
import ProfileLayout from "../Layouts/ProfileLayout";

const MyVideos = ({darkMode}) => {
  return (
    <ProfileLayout darkMode={darkMode}>
      <div>

        My video
      </div>
    </ProfileLayout>
  );
};

export default MyVideos;
